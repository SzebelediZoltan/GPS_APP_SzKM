<Window x:Class="gpass_app_wpf.Views.ClanFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Új klán létrehozása"
        Height="510"
        Width="440"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackground}">

    <Grid Margin="25"
          Background="{DynamicResource SurfaceBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <!-- CÍM -->
        <TextBlock Grid.Row="0"
                   Text="Új klán létrehozása"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="{DynamicResource PrimaryText}"
                   Margin="0,0,0,14"/>


        <!-- HIBA SÁV -->
        <Border Grid.Row="1"
                Background="#3a1f1f"
                BorderBrush="{DynamicResource Accent}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10,7"
                Margin="0,0,0,12"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}">

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠ "
                           Foreground="{DynamicResource Accent}"
                           FontWeight="Bold"/>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{DynamicResource Accent}"
                           TextWrapping="Wrap"
                           FontSize="13"/>
            </StackPanel>

        </Border>


        <!-- FORM -->
        <StackPanel Grid.Row="2">

            <!-- KLÁN NÉV -->
            <TextBlock Text="Klán neve *"
                       Foreground="{DynamicResource SecondaryText}"
                       Margin="0,0,0,3"/>

            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                     IsEnabled="{Binding Saving, Converter={StaticResource InverseBool}}"/>

            <TextBlock Text="{Binding NameError}"
                       Foreground="{DynamicResource Accent}"
                       FontSize="11"
                       Margin="2,0,0,12"
                       Visibility="{Binding HasNameError, Converter={StaticResource BoolToVisibility}}"/>


            <!-- LEÍRÁS -->
            <TextBlock Text="Leírás (opcionális)"
                       Foreground="{DynamicResource SecondaryText}"
                       Margin="0,0,0,3"/>

            <TextBox MaxLength="200"
                     Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                     IsEnabled="{Binding Saving, Converter={StaticResource InverseBool}}"/>

            <TextBlock Text="{Binding DescriptionError}"
                       Foreground="{DynamicResource Accent}"
                       FontSize="11"
                       Margin="2,0,0,12"
                       Visibility="{Binding HasDescriptionError, Converter={StaticResource BoolToVisibility}}"/>


            <!-- VEZETŐ KERESÉS -->
            <TextBlock Text="Vezető keresése *"
                       Foreground="{DynamicResource SecondaryText}"
                       Margin="0,0,0,3"/>


            <!-- KERESŐ -->
            <Grid Margin="0,0,0,5">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding LeaderSearch, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{Binding Saving, Converter={StaticResource InverseBool}}"
                         ToolTip="Keresés felhasználónév vagy email alapján"/>

                <TextBlock Grid.Column="1"
                           Text="⏳"
                           FontSize="14"
                           VerticalAlignment="Center"
                           Margin="7,0,0,0"
                           Foreground="{DynamicResource MutedText}"
                           Visibility="{Binding Searching, Converter={StaticResource BoolToVisibility}}"/>

            </Grid>


            <!-- TALÁLATI LISTA -->
            <ListBox x:Name="leaderSearchListBox"
                     ItemsSource="{Binding SearchResults}"
                     MaxHeight="120"
                     BorderBrush="{DynamicResource SurfaceBorderBrush}"
                     BorderThickness="1"
                     SelectionChanged="LeaderSearchListBox_SelectionChanged"
                     Visibility="{Binding SearchResults.Count, Converter={StaticResource IntToVisibility}}">

                <ListBox.ItemTemplate>
                    <DataTemplate>

                        <StackPanel Orientation="Horizontal" Margin="2,3">

                            <TextBlock Text="{Binding username}"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource PrimaryText}"/>

                            <TextBlock Text="  "/>

                            <TextBlock Text="{Binding email}"
                                       Foreground="{DynamicResource MutedText}"
                                       FontSize="11"/>

                        </StackPanel>

                    </DataTemplate>
                </ListBox.ItemTemplate>


                <ListBox.ItemContainerStyle>

                    <Style TargetType="ListBoxItem">

                        <Setter Property="Foreground"
                                Value="{DynamicResource PrimaryText}"/>

                        <Style.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background"
                                        Value="{DynamicResource AccentHover}"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>

                        </Style.Triggers>

                    </Style>

                </ListBox.ItemContainerStyle>

            </ListBox>


            <!-- KIVÁLASZTOTT VEZETŐ -->
            <Border Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource Accent}"
                    BorderThickness="1"
                    CornerRadius="3"
                    Padding="8,6"
                    Margin="0,5,0,0"
                    Visibility="{Binding HasSelectedLeader, Converter={StaticResource BoolToVisibility}}">

                <StackPanel Orientation="Horizontal">

                    <TextBlock Text="✔ Vezető: "
                               Foreground="{DynamicResource Accent}"
                               FontWeight="Bold"/>

                    <TextBlock Text="{Binding LeaderDisplayText}"
                               Foreground="{DynamicResource PrimaryText}"/>

                </StackPanel>

            </Border>


            <TextBlock Text="{Binding LeaderError}"
                       Foreground="{DynamicResource Accent}"
                       FontSize="11"
                       Margin="2,3,0,0"
                       Visibility="{Binding HasLeaderError, Converter={StaticResource BoolToVisibility}}"/>

        </StackPanel>


        <!-- GOMBOK -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,14,0,0">

            <Button Content="Mégse"
                    Padding="14,7"
                    Margin="0,0,10,0"
                    Command="{Binding CancelCommand}"/>

            <Button Padding="14,7"
                    Background="{DynamicResource Accent}"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Command="{Binding SaveCommand}">

                <Button.Content>

                    <StackPanel Orientation="Horizontal">

                        <TextBlock Text="⏳ "
                                   Visibility="{Binding Saving, Converter={StaticResource BoolToVisibility}}"/>

                        <TextBlock Text="Létrehozás"/>

                    </StackPanel>

                </Button.Content>

            </Button>

        </StackPanel>

    </Grid>
</Window>