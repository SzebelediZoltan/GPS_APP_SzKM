<Window x:Class="gpass_app_wpf.Views.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GPass - Bejelentkezés" Height="540" Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource LoginOuterBg}">

    <Border Background="{DynamicResource LoginCardBg}"
            BorderBrush="{DynamicResource LoginCardBorder}"
            BorderThickness="1" CornerRadius="12"
            Margin="36,26,36,26" Padding="36,24">
        <StackPanel>

            <!-- Logo + témaváltó -->
            <Grid Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⬡" FontSize="22" Foreground="{DynamicResource Accent}"
                               VerticalAlignment="Center" Margin="0,0,7,0"/>
                    <TextBlock Text="GPASS" FontSize="22" FontWeight="Bold"
                               Foreground="{DynamicResource Accent}" VerticalAlignment="Center"/>
                </StackPanel>
                <Button x:Name="themeToggleBtn" Grid.Column="1"
                        Content="🌙" FontSize="13" Width="30" Height="30" Padding="0"
                        Background="{DynamicResource ToggleBtnBg}"
                        Foreground="{DynamicResource ToggleBtnFg}"
                        BorderThickness="1" BorderBrush="{DynamicResource SurfaceBorderBrush}"
                        Click="ThemeToggle_Click" ToolTip="Téma váltása"/>
            </Grid>

            <TextBlock Text="Admin Panel" FontSize="12"
                       Foreground="{DynamicResource MutedText}" Margin="0,0,0,20"/>

            <!-- Hibasáv -->
            <Border Background="#3D1515" BorderBrush="#C53030" BorderThickness="1"
                    CornerRadius="5" Padding="10,8" Margin="0,0,0,12"
                    Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="⚠ " Foreground="#FC8181"
                               FontWeight="Bold" Margin="0,1,6,0"/>
                    <TextBlock Grid.Column="1" Text="{Binding ErrorMessage}" Foreground="#FC8181"
                               FontSize="12" TextWrapping="Wrap"/>
                </Grid>
            </Border>

            <!-- Felhasználó -->
            <TextBlock Text="Felhasználó" FontSize="12" FontWeight="SemiBold"
                       Foreground="{DynamicResource SecondaryText}" Margin="0,0,0,4"/>
            <TextBox Height="36" FontSize="13" Margin="0,0,0,12"
                     IsEnabled="{Binding Loading, Converter={StaticResource InverseBool}}"
                     Text="{Binding UserIdentifier, UpdateSourceTrigger=PropertyChanged}"/>

            <!-- Jelszó -->
            <TextBlock Text="Jelszó" FontSize="12" FontWeight="SemiBold"
                       Foreground="{DynamicResource SecondaryText}" Margin="0,0,0,4"/>
            <PasswordBox x:Name="passwordBox" Height="36" FontSize="13" Margin="0,0,0,12"
                         IsEnabled="{Binding Loading, Converter={StaticResource InverseBool}}"/>

            <!-- Szerver választó - két toggle gomb -->
            <TextBlock Text="Szerver" FontSize="12" FontWeight="SemiBold"
                       Foreground="{DynamicResource SecondaryText}" Margin="0,0,0,6"/>
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Height="44" Margin="0,0,4,0"
                        Command="{Binding SelectServerCommand}"
                        CommandParameter="localhost"
                        IsEnabled="{Binding Loading, Converter={StaticResource InverseBool}}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background"      Value="{DynamicResource ToolbarBackground}"/>
                            <Setter Property="Foreground"      Value="{DynamicResource MutedText}"/>
                            <Setter Property="BorderBrush"     Value="{DynamicResource InputBorder}"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="6">
                                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock Text="💻  Localhost" FontSize="12"
                                                           Foreground="{TemplateBinding Foreground}"
                                                           HorizontalAlignment="Center"/>
                                                <TextBlock Text="localhost:4000" FontSize="10"
                                                           Foreground="{TemplateBinding Foreground}"
                                                           HorizontalAlignment="Center" Opacity="0.65"/>
                                            </StackPanel>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLocalhostSelected}" Value="True">
                                    <Setter Property="Background"  Value="{DynamicResource Accent}"/>
                                    <Setter Property="Foreground"  Value="White"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Grid.Column="1" Height="44" Margin="4,0,0,0"
                        Command="{Binding SelectServerCommand}"
                        CommandParameter="gpass"
                        IsEnabled="{Binding Loading, Converter={StaticResource InverseBool}}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background"      Value="{DynamicResource ToolbarBackground}"/>
                            <Setter Property="Foreground"      Value="{DynamicResource MutedText}"/>
                            <Setter Property="BorderBrush"     Value="{DynamicResource InputBorder}"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="6">
                                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock Text="🌐  Éles szerver" FontSize="12"
                                                           Foreground="{TemplateBinding Foreground}"
                                                           HorizontalAlignment="Center"/>
                                                <TextBlock Text="gpass.site" FontSize="10"
                                                           Foreground="{TemplateBinding Foreground}"
                                                           HorizontalAlignment="Center" Opacity="0.65"/>
                                            </StackPanel>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsGpassSelected}" Value="True">
                                    <Setter Property="Background"  Value="{DynamicResource Accent}"/>
                                    <Setter Property="Foreground"  Value="White"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Bejelentkezés gomb -->
            <Button Height="40" FontSize="14" FontWeight="SemiBold"
                    Background="{DynamicResource Accent}" Foreground="White"
                    BorderThickness="0" Command="{Binding LoginCommand}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⏳ " FontSize="14"
                               Visibility="{Binding Loading, Converter={StaticResource BoolToVisibility}}"/>
                    <TextBlock Text="Bejelentkezés" Foreground="White"/>
                </StackPanel>
            </Button>

        </StackPanel>
    </Border>
</Window>
