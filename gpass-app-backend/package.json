{
  "name": "users",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll",
    "test:coverage": "jest --coverage",
    "start": "nodemon .",
    "db:migrate": "npx sequelize-cli db:migrate --env test",
    "db:migrate:undo": "npx sequelize-cli db:migrate:undo:all --env test",
    "db:seed:all": "npx sequelize-cli db:seed:all --env test",
    "db:seed:undo": "npx sequelize-cli db:seed:undo:all --env test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie": "^1.1.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "express-rate-limit": "^8.2.1",
    "express-rate-limiter": "^1.3.1",
    "express-session": "^1.18.2",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.15.1",
    "nodemailer": "^8.0.1",
    "sequelize": "^6.37.7",
    "socket.io": "^4.8.3",
    "supertest": "^7.2.2",
    "swagger-ui-express": "^5.0.1",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^30.2.0",
    "nodemon": "^3.1.10",
    "sequelize-cli": "^6.6.5"
  }
}
